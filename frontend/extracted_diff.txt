[diff_block_start]\n@@ -6,7 +6,7 @@\n <title>MundialPick — Encontrá tu partido</title>\n <link rel=\"preconnect\" href=\"https://fonts.googleapis.com\">\n <link rel=\"preconnect\" href=\"https://fonts.gstatic.com\" crossorigin>\n-<link href=\"https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Barlow+Condensed:wght@400;600;700&family=Barlow:wght@400;500&display=swap\" rel=\"stylesheet\">\n+<link href=\"https://fonts.googleapis.com/css2?family=Outfit:wght@400;600;800&family=Inter:wght@400;500;600&display=swap\" rel=\"stylesheet\">\n <style>\n   *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }\n \n@@ -13,6 +13,6 @@\n-    --yellow: #F5C800;\n-    --yellow-dim: rgba(245,200,0,0.10);\n-    --yellow-border: rgba(245,200,0,0.22);\n+    --yellow: #4d99fe;\n+    --yellow-dim: rgba(77, 153, 254, 0.10);\n+    --yellow-border: rgba(77, 153, 254, 0.22);\n     --wc-red: #E8231A;\n     --wc-green: #1A7A3C;\n     --wc-blue: #1A3F8F;\n@@ -27,7 +27,7 @@\n   body {\n     background: var(--bg);\n     color: var(--text);\n-    font-family: 'Barlow', sans-serif;\n+    font-family: 'Inter', sans-serif;\n     min-height: 100vh;\n     overflow-x: hidden;\n   }\n@@ -58,7 +58,7 @@\n     opacity: 0.9;\n   }\n   .nav-wordmark {\n-    font-family: 'Bebas Neue', sans-serif;\n+    font-family: 'Outfit', sans-serif;\n     font-size: 24px; letter-spacing: 3px; color: var(--text);\n   }\n   .nav-wordmark span { color: var(--yellow); }\n@@ -65,7 +65,7 @@\n     display: flex; gap: 2rem; list-style: none;\n   }\n   .nav-links a {\n-    font-family: 'Barlow Condensed', sans-serif;\n+    font-family: 'Outfit', sans-serif;\n     font-size: 12px; font-weight: 700; letter-spacing: 1.8px;\n     text-transform: uppercase; color: var(--text-muted); text-decoration: non\n<truncated 7492 bytes>\n-    font-family: 'Bebas Neue', sans-serif;\n+    font-family: 'Outfit', sans-serif;\n     font-size: 28px;\n     letter-spacing: 1.5px;\n     color: var(--text);\n@@ -840,7 +840,7 @@\n     justify-content: flex-end;\n   }\n   .cq-continue-btn {\n-    font-family: 'Barlow Condensed', sans-serif;\n+    font-family: 'Outfit', sans-serif;\n     font-size: 12px;\n     font-weight: 700;\n     letter-spacing: 2.5px;\n@@ -877,7 +877,7 @@\n     min-height: 350px;\n   }\n   .cq-closing-title {\n-    font-family: 'Bebas Neue', sans-serif;\n+    font-family: 'Outfit', sans-serif;\n     font-size: clamp(36px, 6vw, 54px);\n     letter-spacing: 2px;\n     color: var(--text);\n@@ -884,6 +884,6 @@\n   }\n   .cq-closing-subtitle {\n-    font-family: 'Barlow Condensed', sans-serif;\n+    font-family: 'Outfit', sans-serif;\n     font-size: 14px;\n     font-weight: 600;\n     letter-spacing: 2px;\n@@ -916,7 +916,7 @@\n     transition: width 0.1s ease;\n   }\n   .cq-progress-pct {\n-    font-family: 'Bebas Neue', sans-serif;\n+    font-family: 'Outfit', sans-serif;\n     font-size: 32px;\n     color: var(--yellow);\n   }\n@@ -935,7 +935,7 @@\n     animation: fadeDown 0.6s ease both;\n   }\n   .cq-result-summary-title {\n-    font-family: 'Bebas Neue', sans-serif;\n+    font-family: 'Outfit', sans-serif;\n     font-size: 24px;\n     letter-spacing: 1.5px;\n     color: var(--yellow);\n@@ -949,7 +949,7 @@\n     font-size: 14px;\n   }\n   .cq-result-label {\n-    font-family: 'Barlow Condensed', sans-serif;\n+    font-family: 'Outfit', sans-serif;\n     font-weight: 700;\n     text-transform: uppercase;\n     letter-spacing: 1px;\n@@ -962,7 +962,7 @@\n \n   .cq-closing-cta-btn {\n     display: none;\n-    font-family: 'Barlow Condensed', sans-serif;\n+    font-family: 'Outfit', sans-serif;\n     font-size: 13px;\n     font-weight: 700;\n     letter-spacing: 3px;\n[diff_block_end]
[diff_block_start]\n@@ -42,6 +42,17 @@\n     opacity: 0.018; pointer-events: none; z-index: 9999;\n   }\n \n+  /* particles */\n+  #particles-canvas {\n+    position: fixed;\n+    top: 0;\n+    left: 0;\n+    width: 100vw;\n+    height: 100vh;\n+    z-index: 1;\n+    pointer-events: none;\n+  }\n+\n   /* ── NAV ── */\n   nav {\n     position: fixed; top: 0; left: 0; right: 0; z-index: 200;\n@@ -1024,6 +1024,8 @@\n </head>\n <body>\n \n+<canvas id=\"particles-canvas\"></canvas>\n+\n <nav>\n   <div class=\"nav-logo\">\n     <img src=\"data:image/png;base64,\n<truncated 9467 bytes>\npush(new Particle());\n+    }\n+\n+    function drawLines() {\n+      let x1, y1, x2, y2, length, opacity;\n+      for (let i in particles) {\n+        // Connect to mouse\n+        let distMouse = Math.sqrt(Math.pow(mouse.x - particles[i].x, 2) + Math.pow(mouse.y - particles[i].y, 2));\n+        if (distMouse < properties.lineLength + 50) {\n+          opacity = 1 - distMouse / (properties.lineLength + 50);\n+          ctx.beginPath();\n+          ctx.moveTo(mouse.x, mouse.y);\n+          ctx.lineTo(particles[i].x, particles[i].y);\n+          ctx.closePath();\n+          ctx.strokeStyle = `rgba(77, 153, 254, ${opacity * 0.7})`;\n+          ctx.lineWidth = 1;\n+          ctx.stroke();\n+          \n+          // slight pull towards mouse\n+          particles[i].x += (mouse.x - particles[i].x) * 0.015;\n+          particles[i].y += (mouse.y - particles[i].y) * 0.015;\n+        }\n+\n+        for (let j in particles) {\n+          x1 = particles[i].x;\n+          y1 = particles[i].y;\n+          x2 = particles[j].x;\n+          y2 = particles[j].y;\n+          length = Math.sqrt(Math.pow(x2 - x1, 2) + Math.pow(y2 - y1, 2));\n+          if (length < properties.lineLength) {\n+            opacity = 1 - length / properties.lineLength;\n+            ctx.beginPath();\n+            ctx.lineWidth = 0.5;\n+            ctx.strokeStyle = `rgba(77, 153, 254, ${opacity * 0.3})`;\n+            ctx.moveTo(x1, y1);\n+            ctx.lineTo(x2, y2);\n+            ctx.closePath();\n+            ctx.stroke();\n+          }\n+        }\n+      }\n+    }\n+\n+    function loop() {\n+      ctx.clearRect(0, 0, width, height);\n+      for (let i in particles) {\n+        particles[i].position();\n+        particles[i].reDraw();\n+      }\n+      drawLines();\n+      requestAnimationFrame(loop);\n+    }\n+    loop();\n+  </script>\n </body>\n </html>\n[diff_block_end]
[diff_block_start]\n@@ -42,145 +42,120 @@\n     opacity: 0.018; pointer-events: none; z-index: 9999;\n   }\n \n-  /* particles */\n-  #particles-canvas {\n-    position: fixed;\n-    top: 0;\n-    left: 0;\n-    width: 100vw;\n-    height: 100vh;\n-    z-index: 1;\n-    pointer-events: none;\n-  }\n-\n-  /* ── NAV ── */\n-  nav {\n-    position: fixed; top: 0; left: 0; right: 0; z-index: 200;\n-    height: 62px;\n-    display: flex; align-items: center; justify-content: space-between;\n-    padding: 0 2.5rem;\n-    background: rgba(8,8,8,0.88);\n-    backdrop-filter: blur(20px);\n-    border-bottom: 1px solid var(--border);\n-  }\n-  .nav-logo {\n-    display: flex; align-items: center; gap: 10px;\n-  }\n-  .nav-logo img {\n-    height: 32px; width: 32px; object-fit: contain;\n-    filter: brightness(0) invert(1);\n-    opacity: 0.9;\n-  }\n-  .nav-wordmark {\n-    font-family: 'Outfit', sans-serif;\n-    font-size: 24px; letter-spacing: 3px; color: var(--text);\n-  }\n-  .nav-wordmark span { color: var(--yellow); }\n-  .nav-links {\n-    display: flex; gap: 2rem; list-style: none;\n-  }\n-  .nav-links a {\n-    font-family: 'Outfit', sans-serif;\n-    font-size: 12px; font-weight: 700; letter-spacing: 1.8px;\n-    text-transform: uppercase; color: var(--text-muted); text-decoration: none;\n-    transition: color 0.2s;\n-  }\n-  .nav-links a:hover { color: var(--yellow); }\n-  .nav-cta {\n-    font-family: 'Outfit', sans-serif;\n-    font-size: 11px; font-weight: 700; letter-spacing: 2.5px;\n-    text-transform: uppercase; color: var(--bg);\n-    background: var(--yellow);\n-    padding: 9px 22px; border: none; cursor: pointer;\n-    clip-path: polygon(6px 0%, 100% 0%, calc(100% - 6px) 100%, 0% 100%);\n-    transition: opacity 0.2s;\n-  }\n-  .nav\n<truncated 14319 bytes>\n  if (trophy) {\n+        trophy.rotation.y = elapsedTime * 0.2; // Rotating slowly\n+        trophy.position.y = Math.sin(elapsedTime * 0.5) * 0.1;\n+      }\n+\n+      const posAttr = particleSystem.geometry.attributes.position;\n+      for (let i = 0; i < particleCount; i++) {\n+        const idx = i * 3;\n+\n+        posAttr.array[idx + 1] += 0.005;\n+        if (posAttr.array[idx + 1] > 4) {\n+          posAttr.array[idx + 1] = -4;\n+        }\n+\n+        const pX = posAttr.array[idx];\n+        const pY = posAttr.array[idx + 1];\n+\n+        const dx = pX - (mouseState.x * 4);\n+        const dy = pY - (mouseState.y * 3);\n+        const dist = Math.sqrt(dx * dx + dy * dy);\n+\n+        if (dist < 1.5) {\n+          const force = (1.5 - dist) * 0.02;\n+          posAttr.array[idx] += (dx / dist) * force;\n+        } else {\n+          posAttr.array[idx] += (initialPositions[i].x - posAttr.array[idx]) * 0.01;\n+        }\n+      }\n+      posAttr.needsUpdate = true;\n+\n+      renderer.render(scene, camera);\n+    }\n+\n+    animate3d();\n+\n+    window.addEventListener('resize', () => {\n+      camera.aspect = window.innerWidth / window.innerHeight;\n+      camera.updateProjectionMatrix();\n+      renderer.setSize(window.innerWidth, window.innerHeight);\n+      renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));\n+\n+      if (trophy) {\n+        const scaleFactor = window.innerWidth < 768 ? 2.5 : 3.5;\n+        const box = new THREE.Box3().setFromObject(trophy);\n+        const size = box.getSize(new THREE.Vector3()).length();\n+        trophy.scale.setScalar(scaleFactor / size);\n+      }\n+    });\n   </script>\n </body>\n </html>\n[diff_block_end]
[diff_block_start]\n@@ -2560,7 +2560,170 @@\n     loop();\n   </script>\n \n-  \n+  <!-- LIBRERÍAS 3D: Three.js + GLTFLoader -->\n+  <script src=\"https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js\"></script>\n+  <script src=\"https://cdn.jsdelivr.net/npm/three@0.128.0/examples/js/loaders/GLTFLoader.js\"></script>\n+\n+  <script>\n+    // ── ESCENA 3D DE LA COPA ──\n+    const canvas3d = document.getElementById('webgl-canvas');\n+    const scene = new THREE.Scene();\n+\n+    // Cámara top-down para ver la copa desde arriba girando\n+    const camera = new THREE.PerspectiveCamera(5, window.innerWidth / window.innerHeight, 0.1, 100);\n+    camera.position.set(10, 16.2, 4.5);\n+    camera.lookAt(0, 3.5, 0);\n+\n+    const renderer = new THREE.WebGLRenderer({ canvas: canvas3d, antialias: true, alpha: true });\n+    renderer.setSize(window.innerWidth, window.innerHeight);\n+    renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));\n+\n+    // Iluminación azul + mouse light\n+    const ambientLight = new THREE.AmbientLight(0x4d99fe, 20);\n+    scene.add(ambientLight);\n+\n+    const mouseLight = new THREE.PointLight(0x4d99fe, 10, 12);\n+    mouseLight.position.set(0, 0, 0);\n+    scene.add(mouseLight);\n+\n+    const rimLight = new THREE.DirectionalLight(0x1a3f8f, 1000);\n+    rimLight.position.set(0, 2, 4);\n+    scene.add(rimLight);\n+\n+    // Carga del modelo GLB\n+    const loader = new THREE.GLTFLoader();\n+    let trophy = null;\n+\n+    loader.load(\n+      'frontend/world_cup_trophy (1).glb',\n+      function (gltf) {\n+        trophy = gltf.scene;\n+\n+        const box = new THREE.Box3().setFromObject(trophy);\n+        const size = box.getSize(new THREE.Vector3()).length();\n+        const center = box.getCenter(n\n<truncated 2457 bytes>\n const t = clock.getElapsedTime();\n+\n+      mouse.x += (mouse.targetX - mouse.x) * 0.08;\n+      mouse.y += (mouse.targetY - mouse.y) * 0.08;\n+\n+      mouseLight.position.x = mouse.x * 3.5;\n+      mouseLight.position.y = mouse.y * 2.5;\n+\n+      if (trophy) {\n+        trophy.rotation.y = t * 0.1;\n+        trophy.position.y = -0.2 + Math.sin(t * 0.5) * 0.05;\n+      }\n+\n+      const posAttr = particleSystem.geometry.attributes.position;\n+      for (let i = 0; i < particleCount; i++) {\n+        const idx = i * 3;\n+        posAttr.array[idx + 1] += 0.003;\n+        if (posAttr.array[idx + 1] > 3) posAttr.array[idx + 1] = -3;\n+\n+        const pX = posAttr.array[idx];\n+        const pY = posAttr.array[idx + 1];\n+        const dx = pX - (mouse.x * 3);\n+        const dy = pY - (mouse.y * 2);\n+        const dist = Math.sqrt(dx * dx + dy * dy);\n+\n+        if (dist < 1.2) {\n+          const force = (1.2 - dist) * 0.03;\n+          posAttr.array[idx] += (dx / dist) * force;\n+        } else {\n+          posAttr.array[idx] += (initialPositions[i].x - posAttr.array[idx]) * 0.01;\n+        }\n+      }\n+      posAttr.needsUpdate = true;\n+      renderer.render(scene, camera);\n+    }\n+\n+    animate3d();\n+\n+    window.addEventListener('resize', () => {\n+      camera.aspect = window.innerWidth / window.innerHeight;\n+      camera.updateProjectionMatrix();\n+      renderer.setSize(window.innerWidth, window.innerHeight);\n+      renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));\n+      if (trophy) {\n+        const scaleFactor = window.innerWidth < 768 ? 2.5 : 4.5;\n+        const box = new THREE.Box3().setFromObject(trophy);\n+        const size = box.getSize(new THREE.Vector3()).length();\n+        trophy.scale.setScalar(scaleFactor / size);\n+      }\n+    });\n+  </script>\n+\n </body>\n \n+\n </html>\n[diff_block_end]
[diff_block_start]\n@@ -2595,7 +2595,7 @@\n     let trophy = null;\n \n     loader.load(\n-      'frontend/world_cup_trophy (1).glb',\n+      'world_cup_trophy (1).glb',\n       function (gltf) {\n         trophy = gltf.scene;\n \n[diff_block_end]
[diff_block_start]\n@@ -2445,6 +2445,7 @@\n           trophy = gltf.scene;\n           const box = new THREE.Box3().setFromObject(trophy);\n           const size = box.getSize(new THREE.Vector3()).length();\n+          console.log(size)\n           const center = box.getCenter(new THREE.Vector3());\n           trophy.position.x += (trophy.position.x - center.x);\n           trophy.position.y += (trophy.position.y - center.y);\n[diff_block_end]
[diff_block_start]\n@@ -1482,7 +1482,7 @@\n         Recomendador de partidos · FIFA World Cup 2026\n       </div>\n \n-      <h1>Tu Mundial. <em>Tus reglas.</em></h1>\n+    \n \n       <p class=\"hero-sub\" style=\"max-width: 440px;\">\n         Descubrí los partidos perfectos para tu estilo.\n[diff_block_end]
[diff_block_start]\n@@ -2419,12 +2419,12 @@\n       const scene = new THREE.Scene();\n \n       const camera = new THREE.PerspectiveCamera(5, canvas3d.clientWidth / canvas3d.clientHeight, 0.1, 100);\n-      camera.position.set(10, 16.2, 4.5);\n-      camera.lookAt(0, 3.5, 0);\n+      camera.position.set(10, 26, 4.5);\n+      camera.lookAt(0, -2, 0);\n \n       const renderer = new THREE.WebGLRenderer({ canvas: canvas3d, antialias: true, alpha: true });\n       renderer.setSize(canvas3d.clientWidth, canvas3d.clientHeight);\n-      renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));\n+      renderer.setPixelRatio(Math.min(window.devicePixelRatio, 200));\n \n       const ambientLight = new THREE.AmbientLight(0xffffff, 20);\n       scene.add(ambientLight);\n@@ -2431,7 +2431,7 @@\n-      const mouseLight = new THREE.PointLight(0xffffff, 10, 12);\n+      const mouseLight = new THREE.PointLight(0xffffff, 1, 2);\n       scene.add(mouseLight);\n \n-      const rimLight = new THREE.DirectionalLight(0x334455, 1000);\n+      const rimLight = new THREE.DirectionalLight(0x334455, 25);\n       rimLight.position.set(0, 2, 4);\n       scene.add(rimLight);\n \n[diff_block_end]
[diff_block_start]\n@@ -2447,9 +2447,11 @@\n           const size = box.getSize(new THREE.Vector3()).length();\n           console.log(size)\n           const center = box.getCenter(new THREE.Vector3());\n-          trophy.position.x += (trophy.position.x - center.x);\n-          trophy.position.y += (trophy.position.y - center.y);\n-          trophy.position.z += (trophy.position.z - center.z);\n+          // Coloca el centro del objeto exactamente en el origen X y Z, y a la altura 3.5 en Y\n+trophy.position.x += (trophy.position.x - center.x);\n+trophy.position.y += (trophy.position.y - center.y);\n+trophy.position.z += (trophy.position.z - center.z);\n+\n           const scaleFactor = window.innerWidth <= 768 ? 2.5 : 4.5;\n           trophy.scale.setScalar(scaleFactor / size);\n           trophy.traverse((child) => {\n@@ -2456,4 +2456,6 @@\n-              child.material.roughness = 0.9;\n+              child.material.transparent = true;\n+              child.material.opacity = 0.1;\n+              child.material.roughness = 0.5;\n               child.material.metalness = 1.05;\n             }\n           });\n[diff_block_end]
[diff_block_start]\n@@ -115,6 +115,7 @@\n       text-transform: uppercase;\n       color: #777;\n       transition: color 0.3s;\n+      margin-right: -3px; /* Compensa el letter-spacing extra a la derecha */\n     }\n \n     .scroll-indicator:hover span {\n[diff_block_end]
[diff_block_start]\n@@ -90,6 +90,11 @@\n       to { opacity: 1; transform: translateY(0); }\n     }\n \n+    @keyframes fadeInIndicator {\n+      from { opacity: 0; transform: translate(-50%, 20px); }\n+      to { opacity: 1; transform: translate(-50%, 0); }\n+    }\n+\n     /* Botón de continuar animado */\n     .scroll-indicator {\n       position: absolute;\n@@ -96,5 +96,5 @@\n       left: 50%;\n-      transform: translateX(-50%);\n+      transform: translate(-50%, 0);\n       z-index: 4;\n       display: flex;\n       flex-direction: column;\n@@ -102,7 +102,7 @@\n       pointer-events: auto;\n       cursor: pointer;\n       opacity: 0;\n-      animation: fadeIn 2s ease-out 1.5s forwards;\n+      animation: fadeInIndicator 2s ease-out 1.5s forwards;\n     }\n \n     .scroll-indicator span {\n[diff_block_end]
[diff_block_start]\n@@ -403,6 +403,306 @@\n         trophyGroup.scale.setScalar(scale);\n       }\n     });\n+    // =========================================================================\n+    // 8. NUEVA CAPA DE EFECTOS VISUALES ESPORÁDICOS (ANFITRIONES)\n+    // =========================================================================\n+    (function() {\n+      // Crear canvas dedicado para efectos (z-index 0, justo detrás del canvas 3D)\n+      const fxCanvas = document.createElement('canvas');\n+      fxCanvas.id = 'fx-canvas';\n+      fxCanvas.style.position = 'absolute';\n+      fxCanvas.style.top = '0';\n+      fxCanvas.style.left = '0';\n+      fxCanvas.style.width = '100vw';\n+      fxCanvas.style.height = '100vh';\n+      fxCanvas.style.zIndex = '0'; // Detrás de la copa 3D y su niebla\n+      fxCanvas.style.pointerEvents = 'none'; // No interfiere con el scroll o clics\n+      document.body.insertBefore(fxCanvas, document.getElementById('canvas-container'));\n+\n+      const ctx = fxCanvas.getContext('2d');\n+      let width, height;\n+\n+      function resize() {\n+        width = window.innerWidth;\n+        height = window.innerHeight;\n+        fxCanvas.width = width;\n+        fxCanvas.height = height;\n+      }\n+      window.addEventListener('resize', resize);\n+      resize();\n+\n+      let particles = [];\n+\n+      // Bucle de renderizado del Canvas 2D\n+      function loop() {\n+        ctx.clearRect(0, 0, width, height);\n+        ctx.globalCompositeOperation = 'screen'; // Modo fusión cinematográfico brillante\n+        \n+        for (let i = particles.length - 1; i >= 0; i--) {\n+          let p = particles[i];\n+          p.update();\n+          p.draw(ctx);\n+          if (p.dead) {\n+            particles.spli\n<truncated 8059 bytes>\n  ctx.lineWidth = this.size;\n+            ctx.shadowBlur = 12;\n+            ctx.shadowColor = 'rgba(0, 255, 80, 0.6)';\n+            ctx.stroke();\n+            ctx.shadowBlur = 0;\n+          }\n+        }\n+      }\n+\n+      function triggerGreen(variant) {\n+        if (variant === 1) {\n+          particles.push(new GreenParticle(1));\n+        } else if (variant === 2) {\n+          for(let i=0; i<15; i++) {\n+            setTimeout(() => particles.push(new GreenParticle(2)), Math.random() * 2000);\n+          }\n+        } else {\n+          for(let i=0; i<4; i++) {\n+            setTimeout(() => particles.push(new GreenParticle(3)), Math.random() * 1500);\n+          }\n+        }\n+      }\n+\n+      // --- PROGRAMADOR DE EVENTOS ESPORÁDICOS ---\n+      function scheduleNextEffect() {\n+        // Intervalo aleatorio entre 8 y 20 segundos\n+        const delay = 8000 + Math.random() * 12000;\n+        setTimeout(() => {\n+          const color = Math.floor(Math.random() * 3);\n+          const variant = Math.floor(Math.random() * 3) + 1;\n+          \n+          if (color === 0) triggerRed(variant);\n+          else if (color === 1) triggerBlue(variant);\n+          else triggerGreen(variant);\n+          \n+          scheduleNextEffect();\n+        }, delay);\n+      }\n+\n+      // Iniciar el primer efecto a los 4 segundos para sorprender al usuario rápidamente\n+      setTimeout(() => {\n+         const initialColor = Math.floor(Math.random() * 3);\n+         const initialVariant = Math.floor(Math.random() * 3) + 1;\n+         \n+         if (initialColor === 0) triggerRed(initialVariant);\n+         else if (initialColor === 1) triggerBlue(initialVariant);\n+         else triggerGreen(initialVariant);\n+         \n+         scheduleNextEffect();\n+      }, 4000);\n+    })();\n   </script>\n </body>\n </html>\n[diff_block_end]
[diff_block_start]\n@@ -403,8 +403,226 @@\n         trophyGroup.scale.setScalar(scale);\n       }\n     });\n-   \n-   \n+\n+    // =========================================================================\n+    // 8. NUEVA CAPA DE EFECTOS VISUALES 3D Y 2D (ANFITRIONES)\n+    // =========================================================================\n+    (function() {\n+      // --- A. LUCES 3D (ROJO Y VERDE - Canadá y México) ---\n+      const redLight = new THREE.PointLight(0xff0000, 0, 50);\n+      const greenLight = new THREE.PointLight(0x00ff66, 0, 50);\n+      scene.add(redLight);\n+      scene.add(greenLight);\n+\n+      let active3DEffect = null;\n+      let effectStartTime = 0;\n+\n+      // --- B. CANVAS 2D (ESTRELLAS AZULES - USA) ---\n+      const fxCanvas = document.createElement('canvas');\n+      fxCanvas.id = 'fx-canvas';\n+      fxCanvas.style.position = 'absolute';\n+      fxCanvas.style.top = '0';\n+      fxCanvas.style.left = '0';\n+      fxCanvas.style.width = '100vw';\n+      fxCanvas.style.height = '100vh';\n+      fxCanvas.style.zIndex = '0'; // Detrás de la copa 3D y su niebla\n+      fxCanvas.style.pointerEvents = 'none'; // No interfiere con la interacción\n+      document.body.insertBefore(fxCanvas, document.getElementById('canvas-container'));\n+\n+      const ctx = fxCanvas.getContext('2d');\n+      let width, height;\n+\n+      function resize() {\n+        width = window.innerWidth;\n+        height = window.innerHeight;\n+        fxCanvas.width = width;\n+        fxCanvas.height = height;\n+      }\n+      window.addEventListener('resize', resize);\n+      resize();\n+\n+      let blueStars = [];\n+\n+      // --- LÓGICA 3D ---\n+      function trigger3DEffect(variant) {\n+        active3DEffect = variant;\n+     \n<truncated 4956 bytes>\n 255, ${alpha})`;\n+          ctx.shadowBlur = blur;\n+          ctx.shadowColor = `rgba(0, 150, 255, ${alpha})`;\n+          ctx.fill();\n+          ctx.shadowBlur = 0;\n+        }\n+      }\n+\n+      function triggerBlue(variant) {\n+        if (variant === 1) {\n+          for(let i=0; i<6; i++) {\n+            setTimeout(() => blueStars.push(new BlueStar(1)), Math.random() * 800);\n+          }\n+        } else {\n+          blueStars.push(new BlueStar(variant));\n+        }\n+      }\n+\n+      // --- BUCLE DE ANIMACIÓN SECUNDARIO ---\n+      function fxLoop(time) {\n+        // 1. Actualizar luces 3D\n+        update3DLights(time);\n+\n+        // 2. Actualizar Canvas 2D\n+        ctx.clearRect(0, 0, width, height);\n+        ctx.globalCompositeOperation = 'screen';\n+        \n+        for (let i = blueStars.length - 1; i >= 0; i--) {\n+          let dead = blueStars[i].update();\n+          blueStars[i].draw(ctx);\n+          if (dead) blueStars.splice(i, 1);\n+        }\n+        \n+        requestAnimationFrame(fxLoop);\n+      }\n+      requestAnimationFrame(fxLoop);\n+\n+      // --- PROGRAMADOR PRINCIPAL ---\n+      function scheduleNext() {\n+        const delay = 8000 + Math.random() * 7000; // 8 a 15 segundos\n+        setTimeout(() => {\n+          // Decidir si es luz 3D (Rojo/Verde) o Canvas 2D (Azul)\n+          const is3D = Math.random() > 0.5;\n+          const variant = Math.floor(Math.random() * 3) + 1;\n+          \n+          if (is3D) trigger3DEffect(variant);\n+          else triggerBlue(variant);\n+          \n+          scheduleNext();\n+        }, delay);\n+      }\n+      \n+      // Lanzar primer efecto rápido (A los 2.5 segs, una luz 3D para asombrar)\n+      setTimeout(() => {\n+        trigger3DEffect(1);\n+        scheduleNext();\n+      }, 2500);\n+    })();\n   </script>\n </body>\n </html>\n[diff_block_end]
