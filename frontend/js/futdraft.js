// Lógica de Draft eliminada - Simplificación Extrema
// Esta es una plantilla base vacía.

export function initDraft() {
  // Inicialización vacía para no romper imports en main.js
  console.log("Draft visual skeleton loaded. Logic disabled.");
}

export function startDraft(isFanatic = false) {
  // La lógica real de inicialización ahora reside directamente en index.html o se ha simplificado a la maqueta estática.
  console.log("Draft flow triggered visually.");
}
